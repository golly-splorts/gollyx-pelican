#!/bin/bash

export GOLLYX_DRAGON_PELICAN_HOME="${HOME}/golly/pelican/gollyx-dragon-pelican"
export GOLLYX_STAGE="prod"

# These read by the pelican.conf file and exposed to the pelican theme
export GOLLYX_TLD="golly.life"
export GOLLYX_BASE_UI="https://dragon.${GOLLYX_TLD}"
export GOLLYX_BASE_API="https://cloud.dragon.${GOLLYX_TLD}"
export GOLLYX_MAPS_API="https://maps.${GOLLYX_TLD}"
